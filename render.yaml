services:
  - type: web
    name: swimtrack-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./render_start.sh
    envVars:
      - key: SWIMTRACK_DB_PATH
        value: /tmp/swimtrack.db
